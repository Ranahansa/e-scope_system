// this is the sample code for the PublicRoute.jsx file

// import React from 'react';
// import { Navigate, Outlet } from 'react-router-dom';

// const PublicRoute = () => {
//     // Check if user is authenticated (you can modify this based on your auth logic)
//     const isAuthenticated = localStorage.getItem('token');

//     // If user is authenticated, redirect to dashboard or home
//     // If not authenticated, render the public route component
//     return isAuthenticated ? <Navigate to="/dashboard" replace /> : <Outlet />;
// };

// export default PublicRoute;